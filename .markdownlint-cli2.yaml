config:
  # Allow long lines — spec tables and descriptions exceed 80 chars
  MD013: false
  # Allow duplicate headings (FR-xxx sections repeat patterns like "Description", "Trace")
  MD024:
    siblings_only: true
  # Allow trailing punctuation in headings (e.g., "FR-001: Fleet Overview KPI Dashboard")
  MD026: false
  # Allow inline HTML (e.g., for badges or links in README)
  MD033: false
  # Allow compact table separator style (|---|---|)
  MD060: false
